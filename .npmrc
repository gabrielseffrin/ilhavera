# Sem store-dir aqui: ele é definido por env var só dentro do container
# (docker/Dockerfile.dev), para este arquivo continuar válido também no CI.
strict-peer-dependencies=false
auto-install-peers=true
